// import { Nullable } from '@sv-connect/domain';
// import * as Cookies from 'js-cookie';

// export const getCookie = <TData = unknown>(name: string): Nullable<TData> => {
//   const data = Cookies.get(name);
//   return data ? JSON.parse(data) : null;
// };
